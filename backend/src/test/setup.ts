/**
 * Vitest global setup (Sprint 1 placeholder).
 *
 * Phase 2 will bootstrap a disposable Postgres here (the compose `testdb`
 * service) and wire Prisma before the integration suite runs. Sprint 1 runs
 * no database-backed tests, so this file intentionally does nothing.
 */
export {};
