# TrainMate v2 — Frontend Environment Configuration
# Base URL for the TrainMate backend REST API and WebSocket gateway.
# Defaults to http://localhost:3000 if not specified.
VITE_API_URL=http://localhost:3000
